# Build results
/.worktrees/
bin/
obj/
# IDE shim csprojs (Directory.Build.props redirects bin/obj here so the
# DLL outputs don't land inside Unity-scanned package folders).
build/

# IDE
.vs/
.vscode/
*.user
*.suo
*.userprefs

# Rider
.idea/
*.sln.iml

# NuGet
*.nupkg
packages/

# OS
.DS_Store
Thumbs.db

# Test results
TestResults/

# P53 agent rig pointer. Written by the rig coordinator at the bound
# worktree/repository root; never committed — it names a machine-local,
# disposable rig manifest.
.neo-rig

# Agent worktree/session state — never tracked
.claude/
